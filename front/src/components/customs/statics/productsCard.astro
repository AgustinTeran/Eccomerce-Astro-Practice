---
const {id,name,image1,price} = Astro.props;
---

<a href={`/product/${id}`} class="bg-white rounded-lg shadow-md w-72 h-[400px] gap-4 pb-4 text-2xl flex flex-col items-center justify-between cursor-pointer hover:opacity-55">
  <img src={image1} alt={`${name} image`} class="w-full object-fill rounded-t-md h-64" />
  <span class="font-medium text-center px-2 max-h-16 overflow-hidden text-ellipsis">{name}</span>
  <span class="font-medium">${price.toLocaleString('de-DE')}</span>
</a>